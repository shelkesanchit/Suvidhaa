import React from 'react';
import NewConnectionForm from './NewConnectionForm';

const ElectricityNewConnectionsForm = ({ onClose }) => (
  <NewConnectionForm onClose={onClose} />
);

export default ElectricityNewConnectionsForm;
